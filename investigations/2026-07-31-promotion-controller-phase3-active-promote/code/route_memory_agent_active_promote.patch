Documentation-quality diff (like code/promotion_shadow_existing_decision_fix.patch in
../../2026-07-28-promotion-quarantine-controller-model/code/) -- NaVILA-Bench/scripts/
route_memory_agent.py has no clean committed baseline in this dev sandbox to apply
against mechanically (last commit is 2026-07-04; the working tree carries ~5 weeks of
accumulated uncommitted work from many sessions). These are the exact hunks added
2026-07-31, shown as unified-diff-style excerpts for a human/future-Claude to read and
re-apply by hand if needed.

=== Hunk 1: __init__ signature (new parameter) ===

@@ RouteMemoryAgent.__init__ signature @@
         sequential_pair_promotion_use_pre_closure_estimates: bool = False,
+        sequential_pair_promotion_model_active_promote: bool = False,
         sequential_pair_short_baseline_disambiguation: bool = False,

=== Hunk 2: __init__ body (new attribute + transient field) ===

@@ RouteMemoryAgent.__init__ body @@
         self.sequential_pair_promotion_use_pre_closure_estimates: bool = bool(
             sequential_pair_promotion_use_pre_closure_estimates
         )
+        # 2026-07-31 (Phase 3, promote/wait only -- INTEGRATION_PLAN.md,
+        # investigations/2026-07-28-promotion-quarantine-controller-model/):
+        # off by default. When True, _select_sequential_pair_relocalization
+        # replaces ONLY the bounded_evidence vote-aggregation step below with
+        # the learned model's promote-vs-wait call (set externally, once per
+        # attempt, via _active_promotion_model_decision -- see round_trip_
+        # eval.py's promotion-model closure, which scores the same
+        # covisibility_records the shadow path already validated). Deliberately
+        # does NOT touch quality_ok (kept as a hard AND-gate would reproduce
+        # the exact "confidently wrong current freezes next" failure Injection B
+        # exists to fix -- the model's own features already subsume what
+        # quality_ok checks) nor quarantine (that stays on
+        # _record_next_anchor_trend/heuristic control only, per user decision
+        # 2026-07-31 -- this flag has zero effect on _quarantined_anchor_indices).
+        # closure_reject_reason and short_baseline vetoes still apply AFTER the
+        # model's call, unchanged -- those guard "is this reading resolvable at
+        # all", a different question from "should we promote", and are cheap
+        # insurance against exactly the failure classes each was added for.
+        self.sequential_pair_promotion_model_active_promote: bool = bool(
+            sequential_pair_promotion_model_active_promote
+        )
+        self._active_promotion_model_decision: Optional[bool] = None
         self._promotion_alias_stall_counter: dict[int, int] = {}

=== Hunk 3: _select_sequential_pair_relocalization's promote decision block ===

Context: this is the existing quality_ok -> candidate_promote -> closure_reject_veto
-> bounded_evidence-vote -> short_baseline-withhold chain. The model's call is spliced
in between the vote and the short-baseline override, so both existing overrides still
apply on top of it unchanged.

@@ _select_sequential_pair_relocalization @@
             candidate_promote = bool(quality_ok and (close_enough or trend_ok or gate_current_est is None))
-            if closure_reject_reason is not None and not current_unreliable:
+            closure_reject_veto = bool(closure_reject_reason is not None and not current_unreliable)
+            if closure_reject_veto:
                 # A genuine, unresolved current/next mismatch (see the
                 # deferred-reject comment above) counts as an explicit failed
                 # vote regardless of what this attempt's own close_enough/
                 ... [comment unchanged] ...
                 candidate_promote = False
             if gate_current_est is None or self.sequential_pair_promotion_mode != "bounded_evidence":
                 # Nothing to hold onto (current has no estimate at all) -- or
                 # bounded-evidence voting is off -- promote on this attempt alone,
                 # exactly like the original design.
                 promote = candidate_promote
             else:
                 promote = self._record_promotion_vote(next_idx, candidate_promote)
+            # 2026-07-31 (Phase 3, promote/wait only -- see this flag's
+            # docstring at __init__): when active, the learned model's own
+            # promote-vs-wait call REPLACES the bounded_evidence vote result
+            # just computed above -- but the vote call itself still ran
+            # unmodified just above for bookkeeping (vote history, alias-aware
+            # stall counter stay populated even while a benched heuristic vote
+            # isn't driving the real decision), same established pattern the
+            # short-baseline override below already uses. quality_ok/
+            # close_enough/trend_ok are deliberately NOT reapplied here.
+            # closure_reject_veto still applies (a bad current/next cross-check
+            # disagreement is not something the model was trained to see).
+            if self.sequential_pair_promotion_model_active_promote:
+                model_decision = self._active_promotion_model_decision
+                self._active_promotion_model_decision = None
+                if model_decision is not None:
+                    promote = bool(model_decision) and not closure_reject_veto
             # 2026-07-16: applied AFTER both paths above -- offline measurement
             # (investigations/2026-07-16-.../CODE_CHANGE_short_baseline_
             # require_resolution.md Step 0) found the under-resolution problem
             # is dominated by promotions completing via the NORMAL
             # bounded_evidence vote path (19.8% of all promotions lacked 0.3m
             # of travel), not the rarer single-attempt bypass above (4.4%) --
-            # so this must gate both paths, not just the bypass.
-            # _record_promotion_vote's own bookkeeping (vote history, window,
-            # alias-aware stall counter) always runs unmodified above; only the
-            # final decision is overridden here.
+            # so this must gate both paths, not just the bypass. Also applies
+            # after the active-model override above, for the same reason
+            # closure_reject_veto does.
             if withhold_for_unresolved_short_baseline:
                 promote = False

Net effect when sequential_pair_promotion_model_active_promote=False (default):
byte-identical control flow to before -- verified via a synthetic instantiation
test (RouteMemoryAgent(enabled=True) with the flag omitted produces the same
attribute values as pre-change).
